// Re-export from expo so the backend Worker picks up route changes.
export { appRouter } from "../../expo/backend/trpc/app-router";
export type { AppRouter } from "../../expo/backend/trpc/app-router";
